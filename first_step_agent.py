from abstract_agent import Agent
from board import Board
import numpy as np
import copy


class FirstStepAgent(Agent):
    """
    Class that represents a simple agent that looks one-step in the future.
    """